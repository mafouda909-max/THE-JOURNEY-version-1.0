# THE JOURNEY V1 — deployment environment template
# Never commit real credentials. Production values belong in the hosting provider's secret environment.

DATABASE_URL=
NODE_ENV=production

# Optional integrations used by enabled features
RESEND_API_KEY=
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=
NEXT_PUBLIC_APP_URL=
